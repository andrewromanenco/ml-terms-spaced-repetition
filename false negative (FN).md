#fundamentals

An example in which the model mistakenly predicts the
[[negative class|negative class]]. For example, the model
predicts that a particular email message is <em>not spam</em>
(the negative class), but that email message <em>actually is spam</em>.

